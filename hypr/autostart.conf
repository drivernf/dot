# Extra autostart processes
# exec-once = uwsm-app -- my-service

# Set default workspaces
#windowrule = workspace 1 silent, match:class ^(chrome-custom)$
#windowrule = workspace 2 silent, match:class ^(com\.mitchellh\.ghostty)$
#windowrule = workspace 3 silent, match:class ^(code)$
#windowrule = workspace 4 silent, match:class ^(discord)$
#windowrule = workspace 5 silent, match:class ^(chrome-custom)$

# Assign workspaces to specific monitors
#workspace = 1, monitor:DP-1
#workspace = 2, monitor:DP-1
#workspace = 3, monitor:DP-1
#workspace = 4, monitor:HDMI-A-1
#workspace = 5, monitor:HDMI-A-1

# Launch programs
exec-once = ~/.config/hypr/autostart-inet.sh &

# Start immediately
# exec-once = ghostty
# exec-once = code

# Start after internet (non-blocking)
# exec-once = sh -c 'nm-online -q --timeout=30; discord' &
# exec-once = sh -c 'nm-online -q --timeout=30; google-chrome-stable --profile-directory="Profile 1" --class="chrome-work"' &
# exec-once = sh -c 'nm-online -q --timeout=30; google-chrome-stable --profile-directory="Default" --class="chrome-personal"' &
